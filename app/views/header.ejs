<md-toolbar layout="row" class="md-whiteframe-z2">
    <div class="md-toolbar-tools">
        <span class="md-flex"><a ng-href="/">DoJugaad</a></span>
        <span flex></span>

        <!--md-button aria-label="Login" ng-click="">
            <ng-md-icon icon="more_vert"></ng-md-icon>
        </md-button-->
        <% if ( user ) { %>
        <% if (user.name.givenName == 'aashish' || user.name.givenName == 'Ashish') {%>
        <md-button  ng-href="/admin"  aria-label="Admin Panel" class="md-raised ">Panel</md-button>
        <%}%>
        <span class="md-flex"><%=user.name.givenName%></span>
        <md-button  ng-href="/logout"  aria-label="Logout" class="md-raised md-accent">Logout</md-button>
        <% }else { %>
        <md-button  ng-href="/login"  aria-label="Login" class="md-raised md-warn">Login</md-button>
        <% } %>
    </div>
</md-toolbar>
